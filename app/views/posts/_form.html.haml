= simple_form_for @post do |f|
	= f.input :image
	= f.input :title
	= f.input :category_id, collection: @category, prompt: "Select category"
	= f.input :link
	= f.input :description

	= f.button :submit